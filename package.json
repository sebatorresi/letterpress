{
  "name": "letterpress",
  "version": "0.1.0",
  "description": "A letterpress printing effect for the web — individual characters with subtle rotation, displacement, and opacity variations mimicking physical type.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate:css": "bun run scripts/generate-css.ts"
  },
  "devDependencies": {
    "tweakpane": "^4.0.5",
    "typescript": "^5.0.0",
    "vite": "^6.0.0"
  }
}
